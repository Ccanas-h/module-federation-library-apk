import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lib.app',
  appName: 'lib-to-apk',
  // webDir: 'dist'
  webDir: 'dist/shell'
};

export default config;
